// #pragma once
// #include "Tensor.hpp"
//
// namespace cppgrad {
//
// TensorRef add(const TensorRef& a, const TensorRef& b);
// TensorRef mul(const TensorRef& a, const TensorRef& b);
// TensorRef matmul(const TensorRef& a, const TensorRef& b);
// TensorRef relu(const TensorRef& a);
// TensorRef sum(const TensorRef& a);
//
// // Called internally in backprop
// void backward_graph(const TensorRef& root, float grad = 1.0f);
//
// }
